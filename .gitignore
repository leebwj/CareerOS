# dependencies
node_modules/

# build output / generated
dist/
.astro/
.output/

# deploy
.vercel/

# env
.env
.env.*
!.env.example

# os / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# personal source docs (resume sources etc.) — never commit
*.docx
*.doc

# resume content (personal: phone etc.) — generator is public, data is not
apps/resume/resume-data.mjs
apps/resume/out/

# secretary secrets (Gmail app password etc.)
apps/secretary/config.mjs
apps/secretary/out/
apps/secretary/data/

# electron app deps
apps/desktop-pet/node_modules/

# generated task file (machine-specific paths)
apps/secretary/careeros-secretary-task.xml

# but DO commit the final résumé deliverables (public anyway — served on the site)
!apps/resume/dist/
!apps/resume/dist/*.docx
